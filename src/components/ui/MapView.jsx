// "주변 점검" 화면의 지도 컴포넌트 (실제 지도 SDK는 추후 연결)
export default function MapView({ latitude, longitude }) {
  return (
    <div className="w-full h-40 bg-gray-100 rounded-xl flex items-center justify-center text-gray-400">
      지도 영역 (연동 예정)
    </div>
  )
}
